# 数据库配置
# 数据库驱动: sqlite 或 mysql
DB_DRIVER=mysql

# MySQL 配置（当 DB_DRIVER=mysql 时使用）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Wzx112500!
DB_NAME=test_openai

# SQLite 配置（当 DB_DRIVER=sqlite 时使用）
# DB_PATH=users.db

# API密钥加密密钥（可选，如果不设置会自动生成并保存到文件）
# API_KEY_ENCRYPTION_KEY=your_encryption_key_here
